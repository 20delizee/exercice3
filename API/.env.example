# Base de données
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=exercice

# Serveur
PORT=3000
NODE_ENV=development

# Rate limiting (optionnel — valeurs par défaut : 3600000 ms / 100 requêtes)
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX=100
